// use proto_pdk_test_utils::*;
// use starbase_sandbox::create_empty_sandbox;

// This won't work because the macro requires a literal name for the dependency,
// while Deno uses a URL.
// generate_globals_test!("deno-test", "...");
